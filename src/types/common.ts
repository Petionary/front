/**************************************************
 * 공통으로 사용하는 타입
 **************************************************/

export interface IUser {
	email: string;
	name: string;
	nickname: string;
	contact: string;
	profileURL: string;
}
